{
  "name": "pokedex-electron",
  "version": "1.0.0",
  "description": "Application de gestion de Pokédex avec Electron et SQLite",
  "main": "src/main/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run watch\" \"wait-on http://localhost:3000 && electron .\"",
    "build": "electron-builder",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": ["electron", "pokedex", "pokemon"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.2.6",
    "electron-builder": "^24.6.4",
    "jest": "^29.7.0",
    "concurrently": "^8.2.2",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "pokenode-ts": "^1.20.0",
    "sqlite3": "^5.1.7",
    "node-fetch": "^2.7.0"
  }
}